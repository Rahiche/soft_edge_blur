/// Defines the type of a control point in the gradient mask.
enum ControlPointType {
  /// This point is the start of a visible area.
  visible,

  /// This point is the start of a transparent area.
  transparent,
}
